export enum HeaderPicker {
    month = 'month',
    year = 'year',
}

export enum MenuPlacement {
    top = 'top',
    bottom = 'bottom',
}

export enum CMP {
    header = 'header',
    calendar = 'calendar',
    timePicker = 'timePicker',
}
